# /build-fix

Fix build and type errors with minimal diffs. Invokes the `build-error-resolver` agent.

Collects all errors first, then fixes incrementally. No refactoring, no architecture changes — only what is needed to get the build green.

**Usage:**
```
/build-fix
```
